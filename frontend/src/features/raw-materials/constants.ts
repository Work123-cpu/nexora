/** No preset categories — a company picks whatever categories fit their own materials via the
 * category field's "type to add" flow (see CategorySelect.tsx). */
export const RAW_MATERIAL_CATEGORIES: string[] = []
