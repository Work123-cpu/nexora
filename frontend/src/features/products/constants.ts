/** No preset categories — a company picks whatever categories fit their own catalog via the
 * category field's "type to add" flow (see CategorySelect.tsx). */
export const PRODUCT_CATEGORIES: string[] = []
