/** No preset categories — a company picks whatever categories fit their own vendors via the
 * category field's "type to add" flow (see CategorySelect.tsx). */
export const VENDOR_CATEGORIES: string[] = []
